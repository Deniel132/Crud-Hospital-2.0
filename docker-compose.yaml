version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: hospitais_DB
    environment:
      POSTGRES_DB: hospitais_DB
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "1234"
    ports:
      - "3030:5432"
    restart: always